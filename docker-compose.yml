services:
  postgres:
    image: postgres:16
    container_name: cashwise-postgres
    restart: always
    environment:
      POSTGRES_USER: cashwise
      POSTGRES_PASSWORD: cashwise3032
      POSTGRES_DB: cashwise_db
    ports:
      - "5432:5432"
    volumes:
      - ./.postgres:/var/lib/postgresql/data